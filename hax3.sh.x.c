#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -r -f /Users/starplayrx/Documents/GitHub/bigmac/hax3.sh -o hax3 
#endif

static  char data [] = 
#define      opts_z	1
#define      opts	((&data[0]))
	"\307"
#define      text_z	821
#define      text	((&data[149]))
	"\121\212\054\072\006\175\223\161\170\142\034\127\304\027\333\253"
	"\063\055\124\066\364\137\176\201\176\014\021\353\145\334\142\324"
	"\172\354\263\047\366\143\260\054\014\121\171\102\230\140\115\005"
	"\315\133\166\047\133\255\373\212\374\272\227\251\164\202\072\233"
	"\040\332\215\015\257\312\026\105\023\257\332\106\136\277\322\255"
	"\373\210\212\032\346\062\355\230\345\045\371\262\213\076\200\044"
	"\212\203\361\133\127\050\071\073\304\322\075\340\143\012\221\153"
	"\136\377\373\127\305\130\227\104\126\234\243\240\145\160\056\071"
	"\107\335\224\135\041\303\373\302\356\171\036\036\053\207\025\070"
	"\175\340\324\170\337\325\346\204\362\127\105\243\214\242\116\060"
	"\125\213\127\227\351\156\047\321\267\336\107\271\004\110\162\052"
	"\333\265\207\035\037\162\376\115\110\371\205\071\271\032\043\122"
	"\133\314\246\120\215\004\157\063\224\173\332\331\374\245\054\017"
	"\036\124\141\226\234\377\054\330\267\126\015\064\022\202\341\140"
	"\063\277\255\227\174\153\071\226\354\110\262\172\247\324\106\277"
	"\052\174\301\243\360\207\127\361\070\354\314\023\376\324\142\275"
	"\016\374\312\224\006\203\306\205\126\153\005\370\162\047\372\117"
	"\355\173\325\263\070\147\075\052\067\026\222\314\265\332\145\017"
	"\223\241\104\072\001\062\370\042\266\017\055\113\372\177\071\113"
	"\136\313\200\174\267\033\330\027\111\117\105\305\236\234\151\205"
	"\115\370\250\263\176\163\152\375\272\046\016\362\360\010\132\132"
	"\164\247\036\261\347\072\063\066\055\017\214\373\353\304\047\330"
	"\077\174\345\054\012\053\237\046\345\362\175\000\024\153\023\225"
	"\136\224\050\143\205\021\122\105\075\257\135\067\117\171\000\141"
	"\104\257\267\212\132\171\114\355\074\145\144\076\173\106\160\247"
	"\072\316\044\345\141\232\350\026\172\220\377\260\124\371\060\361"
	"\233\210\233\075\033\151\016\021\252\254\363\013\201\030\242\150"
	"\206\127\221\062\213\132\150\104\101\334\046\177\034\154\226\126"
	"\247\314\123\110\127\233\005\312\057\226\065\062\310\166\221\214"
	"\315\270\210\232\366\030\325\244\161\004\257\123\305\170\053\277"
	"\113\272\320\270\301\026\243\064\067\143\246\364\000\207\174\140"
	"\162\240\246\312\171\301\314\302\323\075\257\360\167\251\327\245"
	"\266\216\214\217\110\105\373\376\270\375\242\235\253\033\333\223"
	"\352\161\334\326\313\172\252\173\241\220\131\303\344\317\277\342"
	"\075\150\326\006\134\343\175\242\060\231\242\133\336\100\224\076"
	"\103\067\333\346\050\016\344\256\161\300\126\045\047\007\010\010"
	"\355\244\014\074\365\173\124\066\373\115\122\250\151\014\215\217"
	"\005\175\266\112\054\037\351\325\343\251\261\246\145\313\055\247"
	"\240\160\044\120\217\206\030\233\263\050\357\124\124\241\005\345"
	"\050\220\320\303\240\314\001\057\360\205\374\337\371\067\343\110"
	"\242\327\274\044\223\337\171\277\305\240\026\367\255\260\151\250"
	"\062\327\250\360\251\305\065\167\343\006\271\150\324\240\174\213"
	"\016\066\354\070\213\160\367\003\242\115\101\354\303\140\053\315"
	"\357\251\075\230\313\240\206\265\346\077\105\175\223\350\310\121"
	"\116\052\361\232\006\377\144\213\045\277\176\220\110\065\365\103"
	"\150\350\274\246\117\306\347\056\303\205\316\333\230\373\130\316"
	"\212\017\023\260\036\062\151\171\041\277\317\305\243\127\361\276"
	"\113\232\217\056\157\142\241\106\263\327\100\137\107\306\231\007"
	"\160\101\252\362\200\050\165\153\365\335\326\172\130\151\176\057"
	"\260\343\271\230\016\100\130\370\110\143\372\312\005\206\026\044"
	"\070\205\077\270\071\216\010\305\255\336\327\305\276\124\204\175"
	"\052\065\326\322\243\210\114\365\212\247\037\301\201\026\142\175"
	"\237\341\114\030\043\340\147\361\032\215\052\024\203\200\142\352"
	"\331\065\045\027\130\134\360\072\302\253\264\103\161\155\317\044"
	"\100\355\050\266\213\347\231\016\274\133\170\370\142\023\247\147"
	"\355\167\074\344\205\276\216\125\215\153\354\023\161\021\323\350"
	"\072\304\177\050\211\246\107\043\063\223\313\073\347\336\037\303"
	"\066\342\021\276\212\116\175\305\113\337\320\222\306\245\151\141"
	"\216\227\306\124\060\007\203\121\373\202\260\144\316\014\223\275"
	"\124\120\015\051\341\224\152\310\041\364\014\312\264\111\131\357"
	"\333\121\202\324\002\200\116\364\303\102\014\114\071"
#define      tst1_z	22
#define      tst1	((&data[975]))
	"\272\361\372\210\302\204\347\066\011\233\147\243\343\245\070\255"
	"\120\036\261\270\123\323\343\217\166"
#define      chk2_z	19
#define      chk2	((&data[1000]))
	"\067\360\216\001\210\337\376\167\376\076\324\021\261\055\222\003"
	"\323\214\070\343\174\344\024\135"
#define      xecc_z	15
#define      xecc	((&data[1025]))
	"\147\257\365\125\235\063\237\035\104\342\260\222\110\241\112\001"
	"\004\115\142\056"
#define      tst2_z	19
#define      tst2	((&data[1044]))
	"\031\015\001\313\316\105\346\000\174\376\022\211\175\071\044\220"
	"\257\315\143\276\241\353\204\073"
#define      pswd_z	256
#define      pswd	((&data[1096]))
	"\145\134\200\137\177\050\222\000\273\332\276\340\260\370\274\066"
	"\275\222\127\210\155\251\050\025\276\040\013\341\323\116\050\372"
	"\264\332\050\216\240\317\244\171\236\227\176\332\312\222\207\117"
	"\202\041\117\350\273\214\045\203\212\145\161\164\036\110\272\155"
	"\374\172\075\024\145\305\022\356\131\344\366\302\301\375\010\342"
	"\366\235\356\224\152\231\243\367\070\215\260\003\003\325\037\275"
	"\307\055\166\300\117\102\252\114\164\344\020\144\022\110\336\121"
	"\314\316\366\102\010\367\011\205\116\261\276\145\053\052\160\175"
	"\352\017\366\063\023\040\056\373\242\122\345\335\261\064\152\016"
	"\232\177\252\024\027\326\103\233\305\050\217\150\045\327\204\110"
	"\274\177\236\123\171\063\261\040\356\377\051\243\233\273\076\246"
	"\004\246\302\231\026\077\253\175\153\035\153\373\312\073\101\013"
	"\376\273\170\211\102\237\072\070\002\252\215\165\245\110\015\163"
	"\101\172\047\354\164\047\052\302\143\026\232\165\252\230\165\355"
	"\221\334\172\163\044\052\331\305\220\031\272\120\312\223\271\074"
	"\253\226\231\060\275\254\000\173\263\237\272\164\053\247\077\331"
	"\032\173\326\356\051\011\172\336\122\047\174\145\200\313\330\222"
	"\267\152\067\314\333\153\325\012\206\257\046\006\073\065\002\044"
	"\327\237\103\050\021\200\257\217\130\121\375\324\252\375\321\226"
	"\332\334\010\116\077\071\022\147\154\253\237\074\173\023\233\275"
	"\162"
#define      shll_z	8
#define      shll	((&data[1389]))
	"\035\250\046\302\144\362\145\064\237\175\275"
#define      msg1_z	65
#define      msg1	((&data[1406]))
	"\310\227\036\024\177\114\340\350\040\235\161\277\231\365\202\154"
	"\063\321\201\350\172\355\306\171\331\240\144\154\232\367\205\056"
	"\307\215\340\165\356\301\276\103\205\243\175\020\334\077\236\027"
	"\110\277\236\174\252\302\260\346\060\325\142\357\254\223\072\376"
	"\325\071\044\037\136\323\206\200\313\025\077\357\144\261\273\143"
#define      inlo_z	3
#define      inlo	((&data[1479]))
	"\220\346\130"
#define      lsto_z	1
#define      lsto	((&data[1482]))
	"\237"
#define      chk1_z	22
#define      chk1	((&data[1486]))
	"\365\270\257\161\052\352\213\331\153\014\350\210\265\201\146\117"
	"\067\134\023\345\250\176\221\321\070\230"
#define      msg2_z	19
#define      msg2	((&data[1512]))
	"\130\070\244\360\204\012\054\322\244\374\065\044\201\345\050\050"
	"\150\202\130\220\256\307\060\247\153"
#define      date_z	1
#define      date	((&data[1534]))
	"\304"
#define      rlax_z	1
#define      rlax	((&data[1535]))
	"\236"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/tmp/shc_x.c"); return -1;}
	remove("/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
